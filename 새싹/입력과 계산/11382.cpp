#include <iostream>

using namespace std;

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
  
	unsigned long long a, b, c;
	cin >> a >> b >> c;
	cout << (a + b + c);

	return 0;
}
